---
import { defaultLang, ui } from "@i18n/ui";

const preferedLang = Astro.preferredLocale;

if (null != preferedLang && preferedLang in ui)
  return Astro.redirect(`/${preferedLang}`);

return Astro.redirect(`/${defaultLang}`);
---
